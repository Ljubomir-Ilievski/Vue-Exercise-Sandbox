import { createRouter, createWebHistory } from 'vue-router'

console.log(" i was here as well")
const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [

  ],
})

export default router
